root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 2

# Windows 上 git 会把 sh 脚本转成 CRLF，容器里就跑不起来了
[*.sh]
end_of_line = lf

[*.md]
trim_trailing_whitespace = false
